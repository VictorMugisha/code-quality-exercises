// Make more descriptive function name and correct arguments in array
function getMaximumNumber(...numbers) {
    // Make use of built-in functions
    const maximumNumber = Math.max(...numbers);
    return maximumNumber;
}